# Miniflux-AI

Automates Miniflux feed triage by using OpenAI-powered relevance scoring.

## Quick Start
- Install dependencies: `npm install`
- Configure `.env` with Miniflux and OpenAI credentials
- Run the script: `npm run start`
